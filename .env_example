# Database connection for local Python scripts
DB_HOST=localhost
DB_PORT=2402
DB_NAME=law_db
DB_USER=root
DB_PASSWORD=@12345@

# Vector database
CHROMA_HOST=localhost
CHROMA_PORT=8001
CHROMA_COLLECTION=vbpl_units

# Embedding model
HF_TOKEN=hf_xxx
EMBEDDING_MODEL=truro7/vn-law-embedding

# Crawl source
VBPL_GATEWAY_BASE_URL=https://vbpl-bientap-gateway.moj.gov.vn

